// window.onload = (event) => {
//     const tap = (window.ontouchstart === null) ? 'touchstart' : 'click';

//     const logo = document.getElementById("logo");
//     logo.addEventListener(tap, () => (window.location.href = window.location.href));
// }